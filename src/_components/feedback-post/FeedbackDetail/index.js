import React from 'react';
import { connect } from 'react-redux';

import './style.css';

class FeedbackDetail extends React.Component {
    render() {
        return (
            <div>FeedbackDetail</div>
        );
    }
}

export default connect()(FeedbackDetail);